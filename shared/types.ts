/**
 * Shared types for ObsidiClaw.
 *
 * TODO: Phase 1 — populate this file with the canonical type definitions
 * currently living as inline stubs in orchestrator/types.ts.
 *
 * Types to migrate here:
 *   - RunId
 *   - LifecycleStage
 *   - RunConfig
 *   - RunResult
 *   - AgentMessage (re-export from @mariozechner/pi-coding-agent or wrap it)
 *   - ContextPackage (Phase 5 — context_engine output)
 */
