/**
 * Shared configuration for ObsidiClaw.
 *
 * TODO: Phase 1 — move provider/model config out of orchestrator.ts and into here.
 *
 * Things to define here:
 *   - OllamaConfig: { baseUrl, model, contextWindow, maxTokens }
 *   - OrchestratorConfig: { systemPrompt, toolset, ... }
 *   - LoggerConfig: { dbPath, ... } (Phase 4)
 */
